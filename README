Making a thunderbird addon to anonymize headers.

In the least should:
    -erase all non-necessary headers
    -remove all non-necessary information from required headers (e.g., remove name from: John Smith <example@example.com>)
